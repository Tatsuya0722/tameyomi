# ためよみ！ 技術仕様

この文書は、公開版「ためよみ！」に搭載されたCPU方針の定義、配布データ形式、数値検証を記録するものです。

ゲームを遊ぶために読む必要はありません。ここでは再現・検証を目的として、実装上の上限、記号、数値を明示します。

## 1. 対象

- 公開ページ: [`index.html`](index.html)
- CPUデータ: [`tameyomi-cpu-99-2d3d-f64.bin`](tameyomi-cpu-99-2d3d-f64.bin)
- 状態上限: `C = 99`
- CPUデータのSHA-256:

  ```text
  d396843b108698aac1c0157186c9a4fcb64f9707f52e75477dba1b9e8230844e
  ```

以降、`c` はCPU気力、`h` は人間気力、`m` はその対局で人間が到達した最高気力を表します。

## 2. ゲームの定義

各ラウンドで双方は、攻撃 `A`、防御 `D`、ため `T` のいずれかを同時に選びます。

| 手 | 必要気力 | 気力変化 |
| --- | ---: | ---: |
| `A` 攻撃 | 2 | `-2` |
| `D` 防御 | 1 | `-1` |
| `T` ため | 0 | `+1` |

攻撃とためがぶつかった場合だけ決着します。人間の攻撃とCPUのためなら人間の勝ち、逆ならCPUの勝ちです。それ以外は、選んだ手による気力変化を同時に適用して次ラウンドへ進みます。

実装は有限状態として `0 ≤ c, h ≤ 99` を扱います。気力99では、ためは合法手ではありません。

## 3. 一次目的: 勝敗均衡

通常局面では、人間勝率を `V(c, h)` とする有限ゼロ和確率ゲームを解いています。人間は `V` を最大化し、CPUは最小化します。

各局面では、双方の合法手から最大3×3の利得行列を作ります。即決着のマスには `+1` または `-1` を置き、続行するマスには遷移先 `V(c', h')` を置きます。この局所ゼロ和行列ゲームの均衡混合戦略を更新して、方針と値を数値的に解きます。

公開CPUは、この一次目的の2D方針を、次節の人間必勝領域以外でそのまま使います。

## 4. 人間必勝領域

次の領域では、人間の勝利が確定しています。

```text
L = {(c, h) | h ≥ 2c + 2}
```

人間は攻撃を `c + 1` 回続けられます。一方CPUは、ためを選ぶと直ちに敗北し、攻撃または防御でしのぐたびに少なくとも気力を1失います。CPUの初期気力は `c` なので、`c + 1` 回目の攻撃まで防ぎ切れません。

この領域では勝敗がすでに決まっているため、CPUの二次目的を導入します。

## 5. 二次目的: 人間最高気力の抑制

人間必勝領域 `L` では、状態を `(c, h, m)` に拡張します。

```text
m = 対局中に人間が持った気力の最大値
```

終局時の利得を `m` とし、人間はその期待値を最大化、CPUは最小化します。したがって、CPUは勝敗を変えずに、人間が勝つまでに貯められる最高気力を抑える方針を採用します。

この副次目的は、有限上限99による境界の影響を、初期局面から実質的に遠ざける役割も持ちます。

`L` の外側では、CPUは一次目的で求めた2D勝敗均衡方針に固定します。3D計算は、その固定方針を境界条件として使います。これにより、通常局面の勝敗均衡を変更せず、人間必勝局面に限って二次目的を解きます。

全立方体なら `100³ = 1,000,000` 状態ですが、常に `h ≤ m` なので、計算時は

```text
100 × (100 × 101) / 2 = 505,000
```

状態へ連続に詰めています。

最終計算は割引なし `γ = 1`、Gauss–Seidel更新、局所行列ゲームの支持集合探索、および残差が大きい成分に対するNewton型補正で行いました。最終残差は `10⁻¹²` 未満です。

## 6. 配布binの形式

配布binはリトルエンディアンです。ヘッダは64バイト、以後の各行は `[P(A), P(D)]` のFloat64ペアです。

ための確率は保存せず、実行時に

```text
P(T) = 1 - P(A) - P(D)
```

で復元します。

| byte offset | 型 | 内容 |
| ---: | --- | --- |
| 0 | ASCII 8 B | magic `TYCPU99\\0` |
| 8 | uint32 | format version = 1 |
| 12 | uint32 | cap = 99 |
| 16 | uint32 | 通常2D行数 = 7,550 |
| 20 | uint32 | 必勝3D行数 = 82,075 |
| 24 | uint32 | 通常2D表の開始位置 = 64 |
| 28 | uint32 | 必勝3D表の開始位置 = 120,864 |
| 32 | uint32 | ファイル全長 = 1,434,064 |
| 36--63 | -- | 予約領域、すべて0 |

通常2D表は、CPU気力 `c` 昇順、人間気力 `h` 昇順で、`L` に含まれない局面だけを格納します。行数は7,550です。

必勝3D表は、CPU気力 `c` 昇順、人間気力 `h` 昇順、人間最高気力 `m = h, h + 1, ..., 99` の順で、`L` に含まれる局面だけを格納します。行数は82,075です。

この二つは `100 × 100 = 10,000` 個の2D局面を重複なく分割しています。

## 7. 検証結果

### 3D二次目的

最終3D計算の記録は次のとおりです。

| 項目 | 値 |
| --- | ---: |
| 状態上限 | 99 |
| 使用状態数 | 505,000 |
| 収束基準 | `10⁻¹²` |
| 最終残差 | `3.013555189251675 × 10⁻¹³` |
| sweep数 | 17 |
| 初期局面での人間最高気力の期待値 | 5.756974939405112 |

### 公開binの構造監査

公開binを、2D方針ファイルと3D方針ファイルから再構成した値と全行照合しました。

| 項目 | 結果 |
| --- | ---: |
| 通常2D行の不一致 | 0 |
| 必勝3D行の不一致 | 0 |
| `1 - P(A) - P(D)` と元の `P(T)` の最大差 | `4.1167069753100805 × 10⁻¹³` |
| 確率として実質的に不正な行 | 0 |

### 一次目的の監査

公開binからCPU方針を復元し、人間側の最適反応を上限99の全局面で計算しました。

```text
V(0, 0) = 0.5000000000000002
```

したがって初期局面で検出された搾取余地は

```text
2.220446049250313 × 10⁻¹⁶
```

です。最適反応計算の最終残差は

```text
9.644507414918735 × 10⁻¹³
```

でした。

## 8. 独立検証の範囲

この文書と配布binだけで、次の検証を独立に行えます。

1. SHA-256、ヘッダ、行数、オフセット、全長を照合する。
2. 各行から `P(T) = 1 - P(A) - P(D)` を復元し、確率の妥当性を確認する。
3. 上記の局面順序で2D・3D行を復号する。
4. 復号したCPUに対する人間の最適反応を計算し、初期勝率と残差を監査する。

この文書は、公開済みの実装と配布データを独立に検証できる仕様です。2D均衡計算・3D二次目的計算からbinを再生成するためのソルバーは、この配布物には含めません。

## 9. 実装上の注意

- CPUは各ラウンド開始時に手を抽選し、プレイヤーの入力後に確率を変えません。
- 表のFloat64値はそのまま使い、ためだけを残差で復元します。
- 配布ページはbinをネットワークから読み込むため、ローカルファイルとして直接開くのではなく、HTTPサーバーまたはGitHub Pages経由で開く必要があります。
